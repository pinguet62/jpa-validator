package fr.pinguet62.jpavalidator.model.id.generated.sequence;

import static fr.pinguet62.jpavalidator.util.TestUtils.runCheck;
import static javax.persistence.GenerationType.SEQUENCE;

import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.GeneratedValue;
import javax.persistence.Id;
import javax.persistence.SequenceGenerator;
import javax.persistence.Table;

import org.junit.Test;
import org.junit.runner.RunWith;

import fr.pinguet62.jpavalidator.util.runner.SchemaRunner;
import fr.pinguet62.jpavalidator.util.runner.Script;

@RunWith(SchemaRunner.class)
public class IdSequenceOkTest {

    @Entity
    @Table(name = "SAMPLE")
    public static class Sample {
        @Id
        @GeneratedValue(strategy = SEQUENCE, generator = "foo")
        @SequenceGenerator(name = "foo", sequenceName = "SAMPLE_SEQUENCE")
        @Column(name = "OK", nullable = true)
        Integer ok;
    }

    @Test
    @Script({ "create sequence SAMPLE_SEQUENCE;",
            "create table SAMPLE ( OK int /* not null*/ generated by default as sequence SAMPLE_SEQUENCE primary key );" })
    public void test() {
        runCheck(Sample.class);
    }

}